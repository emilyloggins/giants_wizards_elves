namespace rock_paper_scissors {
    class Program {
        static void Main(string[] args)
        {
            // Initialize both scores as 0
            int computerScore;
            int playerScore;

            // Create a random number and store it in a variable to use as the computer's input.
            int randomNum = random.Next(1,4);
        }
    }
}